# CoutHealth — Decisões (log de suposições em execução autônoma)

> Registrado conforme `escopo.md` §13.0/§11.2 — toda ambiguidade resolvida com um default é anotada aqui.

## 2026-07-17 — Setup de ambiente (pré-Fase 0)

- **Stitch MCP**: o endpoint oficial direto (`https://stitch.googleapis.com/mcp`, header `X-Goog-Api-Key`) conecta, mas o cliente MCP local (Claude Code 2.1.212) falha ao resolver o schema de ferramentas (`$defs/ScreenInstance`). Trocado para o servidor comunitário `@_davideast/stitch-mcp` (modo `proxy`), autenticado via `STITCH_API_KEY` (sem gcloud). Decisão do usuário.
- **Playwright MCP**: instalado via `@playwright/mcp@latest` (stdio), conforme `playwright.dev/mcp/installation`.
- **REPO_GIT**: `https://github.com/guilherme-ufscar/coutinho` — repositório git local inicializado com esse remote.
- ⚠️ **Ação pendente do usuário**: rotacionar a `STITCH_API_KEY` compartilhada em texto puro no chat (Stitch → Profile → Settings → API key), por segurança — ela ficou registrada em `~/.claude.json` local (fora do repo) mas não deve continuar sendo a chave de produção.

## Fase 0 — Fundação

- **Gerenciador de pacotes**: npm workspaces (monorepo simples), em vez de pnpm/turborepo — reduz dependências externas de bootstrap; pode migrar para Turborepo em fase posterior se o build ficar lento.
- **Dev domains**: Caddy local usa `*.localhost` (ex.: `app.localhost`, `api.localhost`) em vez do domínio de produção, para HTTPS automático local sem precisar de DNS público. Produção usa `couthealth.com.br` / `app.couthealth.com.br` / `api.couthealth.com.br` via `.env`.
- **API NestJS na Fase 0**: sobe como esqueleto mínimo (health endpoint) — módulos de domínio completos entram nas fases 3+ conforme o roadmap.
- Demais defaults: ver `escopo.md` §13.0 (fonte Space Grotesk/Inter, PaymentProvider mock+Asaas, YouTube não listado, MinIO, papéis client/professional, etc.) — todos adotados sem alteração.
- **`.env` local**: `DOMAIN`/`APP_DOMAIN`/`API_DOMAIN` default para `*.localhost` (não os domínios de produção) para que o Caddy emita certificado interno automaticamente em dev. `.env.example` documenta a troca para produção.
- **Next.js standalone em Docker**: obrigatório setar `ENV HOSTNAME=0.0.0.0` no Dockerfile — o Docker define `HOSTNAME` automaticamente como o ID do container, e o `server.js` gerado pelo `output: "standalone"` usa `process.env.HOSTNAME` para bind, fazendo o healthcheck falhar silenciosamente (`connection refused` em 127.0.0.1) sem esse override.
- **Backup do Postgres**: trocado cron (`dcron`) por um loop `while true; do backup; sleep 86400; done` — `crond -f` falhava com `setpgid: Operation not permitted` dentro do container (sandboxing). Mais simples e portável para o MVP.
- **Fase 1 (Stitch)**: o Stitch MCP (`@_davideast/stitch-mcp` proxy) segue `✔ Connected` no `claude mcp list`, mas suas ferramentas (`create_project`, `generate_screen_from_text` etc.) não aparecem no `ToolSearch` desta sessão — MCPs adicionados no meio de uma sessão do Claude Code só carregam ferramentas após reiniciar. Sem reinício disponível, a Fase 1 é **pulada como etapa isolada**: as telas são implementadas direto em código a partir do Prompt Base/Design DNA (§7) e dos prompts por tela (§9) de `design-stitch.md`, usados como especificação textual em vez de mockup gerado. Retomar a geração real no Stitch em uma sessão futura é possível a qualquer momento (chave e conexão já configuradas).
- **Validação Playwright da Fase 0**: parcial. Playwright MCP foi instalado no meio desta sessão do Claude Code; o cliente MCP só carrega ferramentas de servidores novos após reiniciar a sessão, então `browser_navigate`/`browser_take_screenshot` etc. não ficaram disponíveis neste turno. `claude-in-chrome` também indisponível (extensão do Chrome não conectada no ambiente). Validação da Fase 0 foi feita via `curl` (status HTTP 200 + payload correto nos 3 domínios). Repetir a validação visual completa (screenshots, console) na próxima fase com UI, quando um dos dois caminhos estiver disponível.

## Fase 2 — Landing (3 variantes)

- **`NEXT_PUBLIC_APP_URL`**: Next.js inlina variáveis `NEXT_PUBLIC_*` no momento do **build**, não do runtime. Passado como `build.args` no `docker-compose.yml` (derivado de `APP_DOMAIN`) em vez de `environment:`, senão o CTA "Criar conta" ficaria hardcoded para `app.localhost` mesmo em produção.
- **Botões de CTA como `<a>`** (não `onClick`): o componente `Button` do design system virou polimórfico (`href` → `<a>`, senão `<button>`) para que as 3 landings continuem Server Components (SEO/performance) — só as seções interativas (`PlansSection` com toggle de período, `FaqSection` com accordion) são Client Components isoladas.
- **`VariantSwitcher`**: navegação flutuante temporária (`/`, `/v2`, `/v3`) nas 3 landings, só para o cliente comparar as direções antes de escolher uma — remover depois da escolha (DoD da Fase 2 no `escopo.md`).
- **Validação Playwright/browser da Fase 2**: mesmo bloqueio da Fase 0 (ferramentas do Playwright MCP não carregadas nesta sessão; `claude-in-chrome` sem extensão conectada). Validação feita via `curl` (status HTTP das 3 rotas) + `next build` sem erros de tipo/lint. Screenshots desktop/mobile pendentes — repetir assim que um caminho de browser real estiver disponível.

## Fase 3 — Auth + Planos + Pagamento

- **Prisma 7 → 5**: `npm install prisma@latest` trouxe a v7, que quebra o padrão `datasource { url = env("DATABASE_URL") }` no schema (exige `prisma.config.ts` + driver adapters). Fixado em `^5.20.0` (estável, compatível com o padrão usado por NestJS/Prisma na maioria dos tutoriais e com o restante do setup) em vez de migrar para o novo modelo de config — reavaliar quando o ecossistema NestJS+Prisma 7 amadurecer.
- **`npm approve-scripts`**: o npm 11 bloqueia scripts de instalação de pacotes novos (argon2, prisma, esbuild) por padrão. Aprovados via `npm approve-scripts --all` (gravado em `allowScripts` no `package.json` raiz) — necessário pro argon2 compilar o binário nativo e o Prisma rodar seus scripts de setup.
- **Dockerfile da API sem `--ignore-scripts`**: diferente dos outros Dockerfiles do monorepo, o da API roda `npm install` completo (scripts habilitados) porque o argon2 precisa compilar via node-gyp (`python3 make g++` instalados no estágio `base`) e o Prisma precisa rodar `prisma generate`. As aprovações já estão persistidas no `package.json`, então não há prompt interativo no build.
- **`docker-entrypoint.sh` da API**: roda `prisma migrate deploy` + `prisma db seed` (idempotente, `|| true`) antes de subir o Nest — garante que um ambiente novo (`docker compose up` do zero) já sobe com schema aplicado e os 3 planos/cupom/admin seedados, sem passo manual.
- **Postgres exposto em `127.0.0.1:5432`** (loopback-only) no `docker-compose.yml`: necessário para rodar `prisma migrate dev` do host durante o desenvolvimento. Mesma instância/volume usada pelo container `api` internamente (`postgres:5432`) — não é um banco separado. Remover/restringir antes de produção (ver Fase 10).
- **Google OAuth sem credenciais reais**: `GoogleStrategy` usa placeholders (`"not-configured"`) em vez de deixar `clientID`/`clientSecret` vazios, porque o `passport-oauth2` lança exceção na construção do provider com string vazia — isso derrubaria a API inteira no boot. Sem credenciais reais, `/auth/google` simplesmente falha no redirecionamento do lado do Google, sem quebrar o resto da aplicação (mesmo espírito do `PaymentProvider` mock: não bloquear por falta de chave real).
- **Asaas real vs. mock**: `AsaasPaymentProvider` implementa a chamada real à API (`/customers`, `/payments`) mas lança `ServiceUnavailableException` explícita se `ASAAS_API_KEY` não estiver setada — diferente do Mock, que sempre aprova. `PAYMENT_PROVIDER=mock` é o default em dev (`.env.example`).
- **Preços**: `PERIOD_DISCOUNT` (mensal 0% / trimestral 8% / semestral 15% / anual 25%) e os valores mensais de cada plano (Essencial 149 / Plus 219 / Elite 349, placeholders) ficam duplicados entre `packages/ui` (preview na landing) e `apps/api/src/payments/payments.service.ts` (cálculo real de cobrança) — a API é a fonte da verdade para o valor cobrado; a landing é só preview de marketing.
- **Fluxo de redirecionamento pós-login**: CTA de plano na landing (`/criar-conta?plano=X&periodo=Y`) → após cadastro/login, `postAuthPath()` manda o usuário para `/planos` com o plano pré-selecionado (fonte de preço real vem da API) → `/checkout` → aprovação automática (mock) libera a assinatura e redireciona para `/app`.
- **`apps/worker/prisma/schema.prisma` duplicado** de `apps/api/prisma/schema.prisma`: cada app roda em container Docker separado (node_modules independentes), então não dá pra compartilhar o Prisma Client gerado sem um pacote comum. Duplicado o schema (o worker nunca roda `migrate`, só lê/escreve via Prisma Client) em vez de criar `packages/db` agora — dívida técnica registrada; extrair para um pacote compartilhado se o schema mudar com frequência nas próximas fases.
## Fase 4 — Onboarding (anamnese única)

- **BullMQ `jobId` não aceita `:`**: `RemindersQueueService` originalmente usava `anamnesis:${userId}` como jobId — quebrava com `Custom Id cannot contain :`. Trocado para `anamnesis-${userId}`. A escrita no Postgres já tinha commitado antes do erro (só o enfileiramento do lembrete falhava), então o bug causava 500 mas sem perda de dado — mesmo assim corrigido antes de seguir.
- **Reagendamento de lembrete a cada save**: em vez de um único lembrete fixo, cada PATCH de rascunho remove o job anterior (`queue.remove(jobId)`) e agenda um novo com `jobId` fixo por usuário — evita empilhar notificações duplicadas enquanto o cliente preenche aos poucos. `submit()` cancela o lembrete pendente.
- **Avaliação física inicial puxada da Fase 7**: o último passo do wizard de anamnese (`escopo.md` §7.1) cria um registro em `Assessment` — implementei um `AssessmentsModule` mínimo (POST/GET) agora em vez de esperar a Fase 7, para não modelar a mesma entidade duas vezes; a Fase 7 só precisa adicionar os gráficos por cima do que já existe.
## Fase 5 — Painel do profissional

- **Cada publicação cria uma nova versão do plano/treino** em vez de editar in-place: `POST /admin/clients/:id/meal-plan` sempre cria um `MealPlan` novo (idem `Workout`); a área do cliente (Fase 6) mostra o mais recente `publishedAt`. Evita lógica complexa de diff/merge em listas aninhadas (refeições→itens, treino→exercícios) para o MVP; histórico de versões fica de graça (todas ficam no banco).
- **Seeds de alimentos/exercícios**: valores nutricionais aproximados de tabelas públicas (TACO/USDA) — marcados como placeholder editável no admin, não são fonte nutricional certificada.
## Fase 9 — APK (Capacitor)

- **Sem Android SDK/Gradle/JDK moderno neste ambiente**: só há JDK 1.8 instalado, sem `ANDROID_HOME`/`gradle`. Buildar o APK localmente não é viável aqui. Em vez de pular a fase, criei `.github/workflows/android-apk.yml` — builda de verdade no GitHub Actions (`ubuntu-latest`, JDK 21, Android SDK 36 via `android-actions/setup-android`) a cada push em `apps/web/**`, publicando o APK debug como artifact. Essa é a validação real de "APK builda" (ver `DECISIONS.md`/`PROGRESS.md` para o link do run).
- **App do cliente empacota o SPA inteiro** (`/app/*` **e** `/admin/*`), não só `/app/*` como o `escopo.md` §4.5 sugere no texto — dividir o bundle exigiria um segundo entry-point/build do Vite. Como `/admin/*` continua protegido por `ProtectedRoute role="PROFESSIONAL"` no cliente **e** por `ProfessionalGuard` na API, um cliente autenticado como `CLIENT` não consegue acessar nada de admin mesmo com o código presente no APK — mitigação aceitável para o MVP; separar os bundles fica como melhoria futura se o tamanho do APK virar problema.
- **Ícone/splash placeholder**: a logo do repo (`logo.webp`) é um wordmark horizontal (2048×439), não um ícone quadrado — inadequado para ícone de app. Gerei um ícone/splash placeholder de marca (fundo `--ink-900`, anel de continuidade em gradiente amarelo, via `apps/web/scripts/generate-app-icons.mjs` + `@capacitor/assets`) até haver um ícone quadrado oficial da marca.
- **Push (FCM)**: estrutura pronta e ativa no app (`@capacitor/push-notifications`, `apps/web/src/lib/push.ts`, roda só em `Capacitor.isNativePlatform()`) e endpoint `POST /client/push-token` salva o token do dispositivo (`User.pushToken`, nova migration). Sem `google-services.json` real (precisa de um projeto Firebase), o registro não dispara — não quebra o app, conforme default do `escopo.md` §13.0 ("ativação em fase posterior").
- **`webDir: "dist"` sem `server.url`**: o app roda com os assets buildados embutidos (offline-first), não carregando a landing/app via URL remota — só chama a API real (`VITE_API_URL` de produção, injetada no build do CI).

- **Validação e2e da Fase 3**: feita via `curl` direto na API rodando localmente contra o Postgres do Docker (cadastro → login → cupom → checkout → assinatura `ACTIVE` confirmada no Postgres). Front (`/criar-conta`, `/planos`, `/checkout`) validado por `vite build` sem erros de tipo; teste de navegador real (Playwright/`claude-in-chrome`) segue pendente pelo mesmo motivo das fases anteriores.

## 2026-07-22 — Mercado Pago configurável pelo admin + Google Pay

- **Apple Pay não incluído**: o Mercado Pago não suporta Apple Pay como método de pagamento no Brasil hoje — confirmado com o usuário. Escopo real ficou só **Google Pay**, via Payment Brick do MP (mostra o botão automaticamente quando o dispositivo/navegador é elegível). Não foi criado nenhum botão "de mentira" nem integrado um segundo gateway (ex. Stripe) só para Apple Pay — custo/complexidade (dois gateways, duas reconciliações) não justificado pelo pedido original.
- ~~**Credencial nunca em `.env`**~~ — **revertido em 2026-07-23**: o usuário pediu para voltar o Access Token do Mercado Pago para `.env` (`MERCADOPAGO_ACCESS_TOKEN`/`MERCADOPAGO_PUBLIC_KEY`), igual ao Asaas. Removida a tabela `PaymentSettings`, a tela Admin → Pagamentos, o `CryptoService` (AES-256-GCM) e a migration que criava a tabela (nunca chegou a rodar contra um Postgres real). `PaymentsService.resolveProvider()` voltou a ser síncrono, lendo só `PAYMENT_PROVIDER` (`mock|asaas|mercadopago`). O Payment Brick + botão Google Pay no `/checkout` foram mantidos.
- **API de Payments (`/v1/payments`) em vez da Orders API**: o Mercado Pago vem migrando a documentação para uma "Orders API" mais nova; optei por manter `/v1/payments` (estável, bem documentada, é o que a maioria das integrações usa hoje) em vez de adotar a API mais nova sem conseguir validar contra credenciais reais neste ambiente. Reavaliar se `/v1/payments` for descontinuada.
- **Validação Playwright pendente**: com o MCP do Playwright agora conectado, a tela `/admin/pagamentos` e o fluxo de `/checkout` ainda não foram abertos num navegador real nesta sessão — falta uma `PUBLIC_KEY`/`ACCESS_TOKEN` de teste do Mercado Pago (o cliente ainda não forneceu) para validar o Payment Brick/botão Google Pay de ponta a ponta.

## 2026-07-23 — Cobrança recorrente mensal (cartão) estilo Claude/Netflix

- **Só cartão é recorrente**: pedido do usuário pra que escolher um período (Trimestral/Semestral/Anual) trave o desconto mas cobre mensalmente, não o total à vista — implementado via API de Assinaturas do Mercado Pago (`/preapproval`, `card_token_id`), nova em `mercadopago-payment.provider.ts` (`createSubscription`/`cancelSubscription`/`getAuthorizedPayment`/`getPreapproval`). **PIX continua exatamente como antes** (pagamento único do valor cheio do período) — Pix Automático do MP é recurso separado/mais restrito, fora de escopo por decisão do usuário.
- **Ao fim do período contratado, expira** (não renova sozinho) — decisão explícita do usuário. Novo job BullMQ `subscription-expiring` (mesma fila `reminders`, mesmo padrão de `anamnesis-incomplete`) agendado em `activateSubscription()`; o worker cancela o preapproval no MP, marca `Subscription.status = EXPIRED` e cria uma `Notification` pedindo pra escolher de novo. `ProtectedRoute`/`hasActiveSubscription` (implementados nesta sessão, ver acima) já bloqueiam `/app` automaticamente quando isso acontece — nenhuma mudança de front necessária pro bloqueio em si.
- **Novo campo `Subscription.mpPreapprovalId`** (migration `20260723130000_subscription_mp_preapproval`) guarda o id da assinatura no MP pra permitir cancelar depois (admin desativando manualmente, expiração automática, ou o próprio cliente cancelando direto no MP via webhook).
- **Webhook novo `POST /payments/webhook/mercadopago`**: valida `x-signature` (HMAC-SHA256 com `MERCADOPAGO_WEBHOOK_SECRET`, novo env) — trata `subscription_authorized_payment` (registra cada cobrança mensal como um novo `Payment`) e `subscription_preapproval` (cliente cancelou direto no MP). **Configurar a URL do webhook no painel do Mercado Pago é passo manual fora do código.**
- **Risco não totalmente validado**: a documentação pública do `POST /preapproval` sobre criar a assinatura já autorizada via `card_token_id` (sem redirecionar o cliente a um `init_point`) estava incompleta nas páginas consultadas — testar em sandbox do MP antes de confiar em produção. Se `status: "authorized"` não vier direto, o `initPoint` retornado já é usado como fallback (`checkoutUrl` no front, mesmo botão "Ir para o pagamento" que existe pro PIX pendente).

## 2026-07-28 — Ajustes do cliente (fluxo de contratação, Google, responsividade)

- **Contratação antes do cadastro**: pedido explícito do cliente para a ordem `Iniciar plano → Contratação → Cadastro → Anamnese → Liberação`. `/planos` e `/checkout` deixaram de exigir JWT (`ProtectedRoute` removido dessas rotas em `App.tsx`) e o cadastro/login virou um passo **embutido** no `CheckoutPage` (`InlineAuth`), exibido enquanto não há sessão. O `POST /checkout` da API continua exigindo JWT — o token passa a existir porque o cadastro acontece imediatamente antes, no mesmo card. Nenhuma mudança de backend foi necessária além do item abaixo.
- **`hadSubscription` no `/auth/me`**: `hasActiveSubscription` sozinho não distinguia "nunca assinou" de "assinou e expirou", então todo usuário novo via a faixa vermelha de "assinatura inativa" (bug reportado pelo cliente com print). `AuthService.publicUser()` passou a expor também `hadSubscription` (existe *qualquer* `Subscription`, independente do status); o `ProtectedRoute` manda `?motivo=novo` ou `?motivo=inativo` e a `PlansPage` alterna entre boas-vindas ("Iniciar plano") e renovação ("Renovar plano"). Campo é **derivado**, não persistido — nenhuma migration.
- **OAuth com redirect preservado**: o botão do Google é um full-page redirect, que perde o estado do React Router. Como o cadastro agora acontece dentro do `/checkout`, foi preciso salvar o caminho em `localStorage` (`savePostAuthRedirect`/`consumePostAuthRedirect` em `lib/redirect.ts`) para o `AuthCallbackPage` devolver o usuário ao checkout com o plano escolhido, em vez de jogá-lo no dashboard.
- **Caddy não roteava `/app` e `/admin` sem barra**: o matcher `path /app/*` **não** casa com a URL sem barra. Acessar `couthealth.com.br/admin` direto, ou dar F5 estando em `/app`, caía na landing e devolvia 404 — e o `ProtectedRoute` redireciona justamente para `/app`. Corrigido listando as duas formas (`/app /app/* /admin /admin/*`). Bug pré-existente, não introduzido nesta sessão.
- **Bind mount de arquivo único não vê `git pull`**: `./infra/caddy/Caddyfile:/etc/caddy/Caddyfile` monta o **inode**, e o `git pull` substitui o arquivo (novo inode) — o container seguiu servindo o Caddyfile antigo mesmo após `docker compose up -d caddy`. Só `docker compose up -d --force-recreate caddy` aplica. Vale para qualquer alteração futura no Caddyfile.
- **Estouro horizontal no mobile**: o `PeriodToggle` era `inline-flex` com `nowrap` — os 4 períodos não cabem em ~340px e empurravam a página inteira, cortando o "Anual -25%" (o maior desconto) e gerando scroll horizontal, visível nos prints do cliente. Passou a quebrar em 2 linhas (`flexWrap`). Somado a isso, `overflow-x: hidden` em `html/body` e `max-width: 100%` em mídia no `tokens.css` como rede de segurança, para que um único elemento largo não volte a quebrar o layout. As tabelas do admin ganharam `.admin-table-wrap` (scroll contido) e a sidebar fixa virou menu hambúrguer abaixo de 860px.
- **Credenciais de pagamento seguem no `.env`** do servidor, sem tela no admin — mantida a decisão de 2026-07-23, confirmada pelo usuário nesta sessão.
- ⚠️ **`MERCADOPAGO_WEBHOOK_SECRET` não está setada em produção**: o `docker compose` avisa a cada comando e a validação de `x-signature` no webhook (`POST /payments/webhook/mercadopago`) não tem como funcionar sem ela. Não bloqueia o checkout, mas as cobranças recorrentes mensais não serão registradas até a chave ser configurada no `.env` e a URL do webhook cadastrada no painel do Mercado Pago.

## 2026-07-31 — Nova rodada de ajustes do cliente (copy, planos, checkout, login Apple)

**Landing — copy revisado pelo cliente** (hero, "como funciona", diferenciais, depoimentos, planos):
- Hero da home: badge "Nutrição • Treino • Saúde", título "Um plano feito para a sua realidade.", CTA primário virou **"Iniciar meu plano"** (mesmo destino de antes, `/criar-conta`). O campo `eyebrow` do `heroCopy.cinematic` ficou **vazio** de propósito (o cliente não forneceu texto de apoio) — o `<p>` só renderiza se houver texto, para não deixar parágrafo vazio no DOM.
- Diferenciais: as 3 imagens (corrida/academia/comida) foram trocadas por consulta com profissional, evolução no celular e treino orientado — o cliente observou que as anteriores representavam atividade física e alimentação, não **acompanhamento**. URLs validadas (200) antes de commitar.
- Depoimentos: 3 nomes fictícios (Camila Rocha, Felipe Andrade, Juliana Martins) no lugar de "Cliente CoutHealth".
- `PlanCard`: badge do destaque virou **"Mais escolhido"** (era "Mais completo"); `ctaLabel` agora é **por plano** (`PlanCopy.ctaLabel`) em vez de "Criar conta" fixo.

**Preços/descontos — landing e API sincronizadas:**
- `PERIOD_DISCOUNT` passou de 8/15/25% para **10/20/40%** (trimestral/semestral/anual) nos **dois** lugares: `packages/ui/PeriodToggle` (preview) e `apps/api/payments.service.ts` (cobrança real). Rótulo do seletor virou "(Economize X%)" com ⭐ no anual, a pedido do cliente (mostrar benefício, não só o percentual). O `flexWrap` já existente no toggle segura os rótulos mais longos sem reintroduzir o estouro horizontal corrigido em 2026-07-28 — cada botão cabe sozinho em ~340px.
- Plus passou de **R$219 → R$249**. Como o seed usa `upsert ... update: {}` de propósito (não sobrescrever edições do admin a cada boot), em banco já existente ele **nunca** atualizaria — a landing anunciaria 249 e a API cobraria 219. Corrigido com a migration `20260731170000_update_plan_copy_and_prices`, que roda uma vez e **só altera as linhas ainda com o texto/preço antigo** (se o admin já editou, preserva).

**Checkout:**
- Rótulo do método virou só **"Cartão"** (era "Cartão / Google Pay"): o Payment Brick continua oferecendo Google Pay quando o dispositivo é elegível, apenas não anunciamos a carteira no botão — pedido do cliente por uma interface mais limpa.
- Texto de recorrência trocado pelo genérico ("renovada automaticamente conforme o período escolhido... pode gerenciar ou cancelar a qualquer momento"), que serve para mensal/trimestral/semestral/anual.
- Nota de segurança 🔒 ("processado com segurança pelo Mercado Pago") acima do botão de pagar.
- ❌ **Apple Pay segue fora** — o cliente pediu de novo, mas a decisão de 2026-07-22 continua válida e foi **reconfirmada na documentação oficial** nesta sessão: o Checkout Pro/Bricks do Mercado Pago aceita no Brasil apenas cartão de crédito/débito, Pix, boleto, Conta Mercado Pago e Linha de Crédito — carteiras suportadas são Google Pay e Samsung Wallet, **não** Apple Pay. Nenhum botão "de mentira" foi criado.
- ⚠️ **"Cartão de Débito Virtual CAIXA" não foi removido**: o cliente pediu que a lista mostrasse só "Cartão", mas quem renderiza essa lista é o Payment Brick do próprio Mercado Pago. A única forma de sumir com aquele item é `debitCard: false` na customização — o que **removeria débito por completo** como meio de pagamento (verificado: assinaturas do MP aceitam débito, então não é código morto). Deixado como está por ser uma decisão de negócio, não técnica — aguardando o cliente escolher.

**Sign in with Apple (novo):**
- Implementado sem `passport-apple` (sem strategy bem mantida) — `AppleService` gera o `client_secret` (JWT ES256 assinado com a chave .p8, expira, por isso é gerado a cada troca) e faz o code exchange. Reaproveita o `JwtService` que já existia; nenhuma lib nova além de `cookie-parser`.
- Callback é **POST** (`response_mode=form_post`, exigido quando se pede escopo), diferente do Google que é GET. O nome do usuário só vem no campo `user` da **primeira** autorização — depois disso, `"Usuário Apple"` como rótulo editável, em vez de bloquear o cadastro.
- CSRF: `state` aleatório em cookie httpOnly `SameSite=None; Secure` (o POST da Apple é cross-site) conferido no callback — daí a necessidade do `cookie-parser`.
- "Ocultar meu e-mail": o relay `@privaterelay.appleid.com` é aceito normalmente; se nem isso vier, gera um placeholder estável a partir do `sub`.
- O `id_token` é decodificado sem revalidar contra o JWKS público **porque vem direto do endpoint da Apple sobre TLS**, autenticado pelo nosso `client_secret` — revalidar só faria sentido se tivesse chegado pelo browser.
- Novo `GET /auth/providers` diz ao front o que está configurado; o botão "Continuar com Apple" **só aparece se houver credenciais** — sem `APPLE_*` no `.env`, nada muda na tela (nada de botão que só daria 503). Falta o cliente criar o Services ID + chave .p8 no Apple Developer Portal.

**Bug pré-existente corrigido (quebrava instalação nova):**
- A migration `20260723120000_only_mercadopago` fazia `DELETE FROM "Payment" WHERE "provider" != 'MERCADOPAGO'` **antes** de o valor existir no enum (que ainda era `('MOCK','ASAAS')` vindo do `init`) — Postgres devolve `22P02 invalid input value for enum` e a migration falha, travando todas as seguintes. Confirmado subindo um banco do zero. Corrigido comparando como texto (`"provider"::text != 'MERCADOPAGO'`). O arquivo de uma migration já aplicada foi alterado, mudando seu checksum — **testado** que isso não afeta produção: com um checksum divergente gravado à força no `_prisma_migrations`, o `prisma migrate deploy` (o que roda no boot do container) responde "No pending migrations to apply" em vez de falhar. Só o `migrate dev` reclamaria.

## 2026-08-05 — Migração Mercado Pago → Stripe (único gateway)

- **Stripe agora é o ÚNICO gateway** — o adapter do Mercado Pago (`mercadopago-payment.provider.ts`) foi deletado, o enum `PaymentProviderName` virou `('STRIPE')` e as envs `MERCADOPAGO_*` saíram do `.env`/`docker-compose` (entram `STRIPE_SECRET_KEY`/`STRIPE_PUBLISHABLE_KEY`/`STRIPE_WEBHOOK_SECRET`). Migration `20260805000000_migrate_to_stripe` converte registros `MERCADOPAGO` → `STRIPE`, renomeia `Subscription.mpPreapprovalId` → `stripeSubscriptionId` e adiciona `User.stripeCustomerId` (Customer do Stripe criado sob demanda no primeiro checkout).
- **Métodos mantidos**: cartão (assinatura recorrente mensal), PIX (pagamento único do período) e carteiras **Apple Pay + Google Pay** — o cliente pediu explicitamente os 4. No Mercado Pago o Apple Pay não era suportado no Brasil (decisão de 2026-07-22); **no Stripe Apple Pay/Google Pay aparecem automaticamente** dentro do checkout quando o dispositivo é elegível (Apple Pay exige verificar o domínio no painel do Stripe — passo manual).
- **Checkout 100% transparente (Embedded Checkout)**: o cliente pediu pagamento direto no site, sem redirecionar para `stripe.com`. Cada aba (`PIX`/`Cartão`) cria uma Checkout Session `ui_mode: 'embedded'` e o `@stripe/stripe-js` a monta num `<div>` dentro do `/checkout` via `stripe.createEmbeddedCheckoutPage({ clientSecret })`. O `return_url` volta para `/checkout?success=1`, onde o front faz polling de `/auth/me` até a assinatura ativar e então segue para a anamnese.
- **Assinatura recorrente = Checkout Session `mode: 'subscription'`** com preço criado por checkout (valor mensal já com desconto de período/cupom, nada de configurar preço fixo no painel). **PIX = `mode: 'payment'` restrito a `payment_method_types: ['pix']`** (impede cartão avulso na aba PIX). O `CheckoutDto` perdeu `token`/`paymentMethodId`/`installments`/`payerDocNumber` — tokenização de cartão é responsabilidade do Stripe agora.
- **Expiração sem job do worker**: o `cancel_at` no fim do período NÃO é aceito em `subscription_data` na API atual do Stripe — é setado depois via `subscriptions.update(cancel_at)` no webhook `checkout.session.completed` (quando já existe o id da assinatura). `customer.subscription.deleted` marca `EXPIRED` + notifica. O job BullMQ `subscription-expiring` e o cancelamento via MP foram **removidos** do worker (que agora só trata `anamnesis-incomplete` e `send-campaign`).
- **Webhook `POST /payments/webhook/stripe`**: valida `stripe-signature` com `STRIPE_WEBHOOK_SECRET` sobre o body bruto (`rawBody: true` no Nest). Eventos tratados: `checkout.session.completed` (ativa + guarda `stripeSubscriptionId` + agenda `cancel_at` + registra 1ª cobrança), `checkout.session.async_payment_succeeded` (PIX pago), `invoice.paid` (cada cobrança mensal), `customer.subscription.deleted` (expirou). `recordPayment` deduplica por `providerChargeId`.
- ⚠️ **Aparência do checkout**: a versão instalada do `@stripe/stripe-js` (v9, `createEmbeddedCheckoutPage`) e do SDK `stripe` (v18) **não aceitam mais o parâmetro `appearance`** — o Embedded Checkout renderiza com o tema claro padrão do Stripe dentro do card escuro. Funcional e 100% no site, mas visualmente destoa do dark; se o cliente quiser, dá pra estilizar (os dois SDKs suportam `theme: 'night'`/variáveis em versões em que o parâmetro existe — reavaliar upgrade conjunto API/JS).
- ⚠️ **Passos manuais obrigatórios no painel do Stripe** (fora do código): habilitar PIX em *Payment methods*, cadastrar o webhook `/api/payments/webhook/stripe` (eventos: `checkout.session.completed`, `checkout.session.async_payment_succeeded`, `invoice.paid`, `payment_intent.succeeded`, `payment_intent.canceled`, `customer.subscription.deleted`) e, para Apple Pay na web, verificar o domínio em *Apple Pay*.
- **Validação**: `npm run build` OK em api/web/worker; migration escrita à mão (sem Docker neste ambiente) seguindo o padrão das anteriores. Teste de ponta a ponta com chaves reais/sandbox do Stripe fica pendente (o usuário precisa criar a conta Stripe e fornecer as chaves + `STRIPE_WEBHOOK_SECRET`).

## 2026-08-05 — Tela PIX customizada + fallback de ativação (bug "pagou mas continua bloqueado")

**Bug crítico reportado (fase Mercado Pago — causa raiz):** o cliente pagou PIX, o dinheiro entrou, mas a assinatura seguiu bloqueada ("assinatura inativa ou expirada"). A causa raiz documentada em 2026-07-23 era a mesma que a do MP: a ativação dependia **100% do webhook** e o `MERCADOPAGO_WEBHOOK_SECRET` nunca foi configurado — sem webhook, pagamento aprovado nunca ativa a assinatura. Na migração para Stripe isso foi endereçado com **três camadas**:
1. **Webhook** `payment_intent.succeeded` ativa a assinatura (idempotente — só ativa se `PENDING`), registra `startedAt`/`currentPeriodEnd` (vencimento = período contratado, `PERIOD_MONTHS`) e vincula o `Payment` ao `Subscription`→`User` (já criados com `userId` do JWT).
2. **Fallback por polling (novo `GET /payments/status/:subscriptionId`)**: o front consulta a cada ~4s; se o Stripe disser que o PIX está `paid` e a assinatura ainda `PENDING`, o próprio endpoint **ativa a assinatura** mesmo que o webhook nunca chegue. Isso elimina de vez o cenário "pagou mas continua bloqueado".
3. **`hasActiveSubscription`** (query do `/auth/me`) passa a retornar `true` assim que ativa → `ProtectedRoute` libera `/app` e some a faixa vermelha — sem logout manual.

**PIX deixou de usar o Embedded Checkout e virou tela 100% customizada** (as 12 melhorias pedidas pelo cliente):
- Backend cria **PaymentIntent de PIX direto** (`createPixPaymentIntent`, `payment_method_types: ['pix']`, `expires_at` = 30 min) e devolve `next_action.pix_display_qr_code` (payload + imagem PNG). `POST /payments/pix/regenerate/:subscriptionId` gera um **novo QR para a mesma assinatura pendente** (sem duplicar Subscription; cancela os PIX antigos).
- Front (`CheckoutPage`): QR + **copiar código PIX** (📋), **countdown** de expiração, **status visível** (🟡 aguardando / 🔄 verificando / 🟢 confirmado / 🔴 expirado), **barra de etapas** (QR gerado → aguardando → liberando → redirecionando), **polling a cada 4s** + botão **"Já paguei"**, **última verificação** (há Xs), aviso de **conexão instável** (offline), **persistência** (localStorage `couthealth:pix-pending` retoma o mesmo pagamento após recarregar — o status devolve o QR se ainda pendente), **animação de confirmação + redirecionamento 3…2…1** para a anamnese, **menu mínimo** (voltar ao site), atalhos (voltar ao início / suporte) e FAQ "enquanto aguarda".
- Cartão continua no **Embedded Checkout** (com `?success=1` + polling do `/auth/me`).
- ⚠️ O **`expires_at` do PIX foi fixado em 30 minutos** (constante `PIX_EXPIRATION_MS`) — se o cliente quiser outro prazo, é uma linha.
- **Validação**: build OK api/web. Fluxo real ainda depende das chaves Stripe + webhook configurado (listado acima).

## 2026-08-11 — Anamnese não "engatilha" após cadastro + pagamento (contexto de auth stale)

**Bug reportado pelo cliente:** após cadastro e pagamento, a anamnese não aparece e o cliente "não tem acesso".

**Causa raiz:** o `AuthContext.user` fica **stale** depois do pagamento. O usuário nasce sem assinatura (`hasActiveSubscription: false` no contexto); ao pagar:
- **PIX**: o polling detecta `paid` e navega para `/anamnese` **sem atualizar o contexto** → `ProtectedRoute requireSubscription` vê o `user` antigo (sem assinatura) e redireciona de volta para `/planos?motivo=novo` — o cliente paga e volta para os planos, em loop.
- **Cartão**: o polling de `?success=1` chamava `authApi.me()` e navegava, mas **também sem gravar no contexto** → mesmo redirect.
- Só "funcionava" com um reload da página (o `AuthProvider` refaz `/auth/me` no mount) — por isso parecia intermitente.

**Correção (3 camadas, `apps/web`):**
1. `lib/auth.tsx`: novo `refreshUser()` no `AuthProvider` — busca `/auth/me` com o token atual e atualiza o `user` do contexto (sem trocar tokens).
2. `CheckoutPage.tsx`: antes de redirecionar para `/anamnese`, confirma a liberação ao vivo via `refreshUser()` — PIX com retries (até 10×1s, voltando ao estado pendente se não confirmar) e cartão no polling do `?success=1`.
3. `ProtectedRoute.tsx` (defesa em profundidade): quando o contexto diz "sem assinatura", faz uma **confirmação ao vivo** (uma vez por montagem, renderizando null durante) antes de redirecionar para `/planos` — cobre qualquer outro caminho com contexto stale (webhook ativando enquanto o usuário navega, abas abertas etc.).

**Bug adicional encontrado e corrigido no mesmo fluxo:** o init do PIX no `CheckoutPage` rodava mesmo com `?success=1` (retorno do cartão) — criava uma `Subscription` PENDING órfã + PaymentIntent fantasma no Stripe a cada volta do Embedded Checkout. Agora o init do PIX é pulado quando `success=1`.

**Validação (browser real, API stubada):** app web buildado + stub da API simulando o backend (login, `/auth/me` com flag `paid` alternável, checkout PIX com ativação no status, webhook manual). Percorridos os dois fluxos ponta a ponta: **PIX** (planos → checkout → QR → "pago" → ativação → anamnese 1/9 renderizada) e **cartão** (`?success=1` → cadastro → sem criação de PIX (`checkouts: 0`) → ativação via webhook simulado → polling → anamnese). Antes da correção, ambos terminavam em `/planos?motivo=novo`.

## 2026-08-11 — Fluxo de entrada do cliente (pedido do cliente: planos → pagamento → conta → anamnese → análise → publicação)

O cliente descreveu o fluxo de entrada completo. Mapeamento do que já existia vs. o que foi implementado:

| Etapa do fluxo | Estado |
|---|---|
| Escolha do plano (Essencial/Plus/Elite + período) | ✅ já existia |
| Pagamento (PIX customizado + cartão via Embedded Checkout) | ✅ já existia |
| **Apple Pay / Google Pay** | ⚠️ **sem código**: o Embedded Checkout do Stripe exibe Apple Pay/Google Pay automaticamente quando habilitados no **Dashboard do Stripe** (Payment Method Configuration) e com o domínio verificado — configuração, não implementação |
| Cadastro da conta + ativação automática pós-pagamento | ✅ já existia (conta criada no checkout com **consentimento LGPD** e ATIVADA automaticamente após o pagamento; login/cadastro Google e Apple já implementados) |
| Anamnese: solicitação automática + **notificação** | 🆕 **implementado** — `activateSubscription` agora chama `notifyAnamnesisIfPending`: se a anamnese não está concluída (nunca criada ou RASCUNHO), cria a notificação "Complete sua anamnese" (sem duplicar se já houver pendente não lida) e agenda o **primeiro** lembrete |
| Anamnese pendente: **lembretes repetidos até concluir** | 🆕 **implementado** — o worker, ao disparar o lembrete com anamnese ainda RASCUNHO, **reagenda +24h** (mesmo jobId `anamnesis-<userId>`, padrão remove+add do `RemindersQueueService`); o `submit()` continua cancelando o job. Antes, o lembrete só existia se o usuário tivesse interagido (PATCH) e era one-shot |
| Anamnese pendente visível na plataforma | 🆕 **implementado** — banner amarelo "Complete sua anamnese" + CTA "Preencher agora" no topo do `/app` (dashboard) quando `status === RASCUNHO` |
| Recebimento automático no painel admin | ✅ já existia (lista de clientes com status da anamnese + detalhe completo) |
| Análise profissional (IA como apoio, decisão humana) | ✅ processo manual já existente; IA continua sendo camada futura (escopo.md §10) |
| Publicação + notificação "plano disponível" | ✅ já existia (`PLANO_PUBLICADO` / `TREINO_ATUALIZADO`) |

**Decisão de produto (cadastro pós-pagamento):** o cliente descreveu "a conta é criada após a confirmação do pagamento". Mantido o fluxo atual — conta criada **no checkout** (com consentimento explícito LGPD, obrigatório para dados de saúde) e **ativada automaticamente** quando o pagamento é confirmado. Pagar antes de cadastrar exigiria coletar consentimento de dados de saúde depois do pagamento, o que contraria o escopo.md §9 — o resultado funcional é o mesmo ("conta criada e ativada automaticamente").

**Validação:** `npm run build` completo (API com dist limpo — o `tsc incremental` estava mascarando build parcial, forçado `rm -rf dist + tsbuildinfo`) + **teste de integração em memória** dos 5 cenários da ativação (RASCUNHO → notificação+lembrete; ENVIADA → nada; pendente existente → sem duplicata; sem anamnese → notificação; já ACTIVE → idempotente) + **validação visual no browser** (stub da API): banner "Complete sua anamnese" renderizado no dashboard com contraste OK.

## 2026-08-11 — Ajustes admin: remoção da Biblioteca + cadastro manual de clientes + anamnese detalhada (PDF aprovado)

**1. Biblioteca removida (admin + cliente)** — pedido do cliente: a seção não existe mais em lugar nenhum.
- Front: rotas `/app/biblioteca` e `/admin/biblioteca` removidas (App.tsx), links de navegação removidos (ClientLayout/AdminLayout), páginas deletadas, `libraryApi` removido do api.ts.
- Backend: `LibraryModule` removido do app.module, pasta `src/library/` deletada.
- Schema: `model LibraryContent` + enum `LibraryContentType` removidos dos schemas da API e do worker; migration `20260811090000_remove_library` (DROP TABLE + DROP TYPE). `exerciseLibrary` (banco de exercícios) NÃO foi tocado.

**2. Cadastro manual + remoção de clientes no admin (recepção)** — pedido do cliente ("adicionar e remover cadastros, a fim de organização").
- `POST /admin/clients`: cria cliente CLIENT com nome/e-mail/senha inicial (argon2), `consentedAt` registrado (consentimento colhido na recepção, trilha de auditoria `CREATE_CLIENT`). O cliente usa e-mail+senha para entrar.
- `DELETE /admin/clients/:id`: **anonimiza** a conta (LGPD art. 16 — histórico de pagamentos/planos retido sem identificar), cancela assinaturas ACTIVE (inclusive `cancelSubscription` no Stripe para parar a cobrança recorrente) e a conta some da lista (`listClients` filtra `email startsWith deleted-`). Audit `REMOVE_CLIENT`.
- Front: botão "+ Novo cliente" (formulário inline no header) + botão "Remover" por linha (com `window.confirm` explicando desativação/cancelamento de cobrança). `AdminClientsModule` agora importa `PaymentsModule` (StripePaymentProvider).

**3. Anamnese detalhada (estrutura aprovada no PDF "Anamnese Detalhada")** — redesenho completo da experiência:
- **6 etapas** (Sobre você / Sua saúde / Sua alimentação / Sua rotina / Seu treino / Sua avaliação), **uma pergunta por tela**, progresso "Etapa X de 6" + anel, intro de conversa por etapa ("Vamos começar pelo mais importante…").
- **Perguntas condicionais**: gates booleanos novos no schema (`hasDiseases`, `usesMedications`, `hasAllergies`, `hasIntolerances`, `hasNutritionalDeficiencies`, `hadSurgeries`, `hasFamilyHistory`, `hasOrthopedicIssues`, `hasAlteredExams`, `usesSupplements`, `practicesActivity`, `hasBioimpedance`) — o detalhe só aparece se "Sim" (ex.: medicamentos). `smokes`/`drinksAlcohol` já eram Boolean.
- **Opções prontas**: choice (objetivo com 6 opções incl. `COMPOSICAO_CORPORAL` e `OUTRO` no enum Goal; sono; função intestinal; refeições 2-7+; dias de treino 1-7), chips de alimentos ("Não encontrou algum alimento? Adicione aqui"), sliders (água 0,5-5L; horas de sono 3-12h).
- **Campos de detalhe novos** (String?): `goalImprove`, `currentDiet`, `foodsAvoided`, `workRoutine`, `mealsOut`, `routineDifficulties`, `sedentarySince`, `trainingHistory`, `enjoyedExercises`, `pain`, `limitations`, `injuries`. Assessment ganhou `heightCm` e `chestCm` (peitoral — pergunta condicionada ao sexo masculino).
- **Salvamento automático**: PATCH a cada avanço/volta ("Seu progresso foi salvo."), `currentStep` = índice na lista linear de perguntas; retomada exata de onde parou (validado no browser). Rascunho da etapa 6 (avaliação) persiste no localStorage `couthealth:anamnesis-assessment` (criado no submit via `POST /assessments`) — limitação registrada: avaliação só é persistida no envio, por design (campos opcionais, preenchíveis depois em Evolução).
- **Tela final**: "Tudo certo! … **Você fez a sua parte. Agora é comigo.**"
- Migration `20260811100000_anamnese_detalhada` (24 colunas na Anamnesis + 2 no Assessment + 2 valores no enum Goal). Admin: `anamnesisLabels` ampliado para exibir todos os campos novos de forma organizada.
- O worker NÃO recebeu os campos novos (schema duplicado não usa anamnese).

**Validação:** `npm run build` completo limpo (dist/tsbuildinfo apagados — tsc incremental estava mascarando build parcial) + teste de integração da ativação (5 cenários, continua passando) + **validação no browser real (Playwright + API stub)**:
- Anamnese: percorrida ponta a ponta (etapa 1 → condicional Sim → chips "Arroz, Frango" → slider → tela final) — persistência conferida no stub (`status: ENVIADA`, `mealsPerDay: 4`, `preferredFoods: "Arroz, Frango"`, `trainingDaysPerWeek: 3`, etc.); "Responder depois" + retomada exata confirmadas.
- Admin: login profissional → lista sem Biblioteca → "+ Novo cliente" (Carlos Pereira criado e listado) → "Remover" (confirm → some da lista; Ana/Bruno intactos).
